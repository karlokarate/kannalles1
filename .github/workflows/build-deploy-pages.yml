name: Build, prove and deploy KH Checker to Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Validate architecture and runtime · Node ${{ matrix.node-version }}
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    strategy:
      fail-fast: false
      matrix:
        node-version: ["22.18.0", "24.18.0"]
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Set up Node.js runtime
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: |
            package-lock.json
            Catalog/runtime/package-lock.json

      - name: Install locked application dependencies
        run: npm ci --no-audit --no-fund

      - name: Install locked SQLite-WASM runtime
        run: npm ci --prefix Catalog/runtime --no-audit --no-fund

      - name: Validate version, production catalog, workflow and browser-support contracts
        run: |
          npm run check:version
          npm run check:catalog
          npm run check:workflow
          npm run check:browser-support

      - name: Typecheck application
        run: npm run typecheck

      - name: Lint browser and test sources
        shell: bash
        run: |
          set -o pipefail
          npm run lint 2>&1 | tee lint-output.log

      - name: Upload lint diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v4
        with:
          name: lint-output-node-${{ matrix.node-version }}
          path: lint-output.log
          if-no-files-found: ignore
          retention-days: 3

      - name: Run targeted catalog unit runtime tests
        shell: bash
        run: |
          set -o pipefail
          npm run test:runtime 2>&1 | tee runtime-test-output.log

      - name: Run targeted PWA deploy update tests
        shell: bash
        run: |
          set -o pipefail
          npm run test:pwa-update 2>&1 | tee pwa-update-test-output.log

      - name: Run targeted semantic input tests
        shell: bash
        run: |
          set -o pipefail
          npm run test:semantic-input 2>&1 | tee semantic-input-test-output.log

      - name: Upload targeted diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v4
        with:
          name: targeted-test-output-node-${{ matrix.node-version }}
          path: |
            runtime-test-output.log
            pwa-update-test-output.log
            semantic-input-test-output.log
          if-no-files-found: ignore
          retention-days: 5

      - name: Run complete deterministic and contract test suite
        run: npm test

      - name: Enforce architecture and dead-code gates
        run: |
          npm run check:architecture
          npm run check:knip

  build:
    name: Build manifest-derived Pages artifact
    needs: quality
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Set up Node.js 24 LTS
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: "24.18.0"
          cache: npm
          cache-dependency-path: |
            package-lock.json
            Catalog/runtime/package-lock.json

      - name: Install locked application dependencies
        run: npm ci --no-audit --no-fund

      - name: Install locked SQLite-WASM runtime
        run: npm ci --prefix Catalog/runtime --no-audit --no-fund

      - name: Validate production catalog artifacts
        run: npm run check:catalog

      - name: Build production PWA
        shell: bash
        run: |
          set +e
          npm run build > build-output.log 2>&1
          status=$?
          tail -n 200 build-output.log
          exit "$status"

      - name: Verify manifest-derived Pages artifact
        run: npm run check:pages

      - name: Upload compiler log on failure
        if: failure()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v4
        with:
          name: offline-build-output
          path: build-output.log
          if-no-files-found: ignore
          retention-days: 3

      - name: Configure GitHub Pages
        if: github.event_name != 'pull_request'
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5
        with:
          path: dist

  browser-e2e:
    name: Prove installed updates, SQLite-WASM and OPFS journeys
    needs: quality
    runs-on: ubuntu-24.04
    timeout-minutes: 65
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Set up Node.js 24 LTS
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: "24.18.0"
          cache: npm
          cache-dependency-path: |
            package-lock.json
            Catalog/runtime/package-lock.json

      - name: Install locked application dependencies
        run: npm ci --no-audit --no-fund

      - name: Install locked SQLite-WASM runtime
        run: npm ci --prefix Catalog/runtime --no-audit --no-fund

      - name: Install Playwright browser engines
        run: npm run test:e2e:install

      - name: Prove update from an already installed deployment
        shell: bash
        run: |
          set -o pipefail
          npm run test:e2e:pwa-update 2>&1 | tee pwa-update-e2e-output.log

      - name: Run targeted real unit runtime journeys
        run: npm run test:e2e:runtime

      - name: Run targeted natural-language quantity journeys
        run: npm run test:e2e:semantic-input

      - name: Run complete real browser catalog proof
        run: npm run test:e2e

      - name: Upload Playwright evidence on failure
        if: failure()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v4
        with:
          name: playwright-catalog-evidence
          path: |
            pwa-update-e2e-output.log
            playwright-report
            playwright-report-pwa-update
            test-results
          if-no-files-found: ignore
          retention-days: 5

  deploy:
    name: Deploy KH Checker PWA
    if: github.event_name != 'pull_request'
    needs: [build, browser-e2e]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout manifest for deployment summary
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5

      - name: Deployment summary from production manifest
        shell: bash
        env:
          PAGE_URL: ${{ steps.deployment.outputs.page_url }}
        run: |
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const manifest = JSON.parse(fs.readFileSync('Catalog/catalog-manifest.v1.json', 'utf8'));
          const lines = [
            '## KH Checker offline catalog deployed',
            '',
            `- URL: ${process.env.PAGE_URL}`,
            `- Commit / app build: ${process.env.GITHUB_SHA}`,
            `- Catalog: ${manifest.catalogVersion} · ${manifest.database.products} products · ${manifest.database.bytes} bytes`,
            `- Database file: ${manifest.database.file}`,
            `- SQLite SHA-256: ${manifest.database.sha256}`,
            '- Installed apps check app-update.json and sw.js without cache on launch.',
            '- Product source: deployed SQLite → A/B OPFS slots → SQLite-WASM worker',
            '- Online OFF/Search-a-licious product access: disabled',
            '',
          ];
          fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, lines.join('\n'));
          NODE
